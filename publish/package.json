{
  "name": "publish",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "clean": "rimraf dist && mkdir dist",
    "makezip": "ts-node makezip && ls dist",
    "main": "ts-node main",
    "start": "npm run clean && npm run makezip && npm run main",
    "test": "cross-env APPLICATION_NAME=pocket-cards-codedeploy-backend PROJECT_NAME=pocket-cards FUNCTION_ALIAS=dev AWS_DEFAULT_REGION=ap-northeast-1 ts-node main"
  },
  "devDependencies": {
    "@types/js-yaml": "^3.12.1",
    "@types/node": "^12.0.2",
    "@types/rimraf": "^2.0.2",
    "archiver": "^3.0.0",
    "aws-sdk": "^2.458.0",
    "cross-env": "^5.2.0",
    "js-yaml": "^3.13.1",
    "mkdirp": "^0.5.1",
    "rimraf": "^2.6.3",
    "ts-node": "^8.1.0",
    "typescript": "^3.4.5"
  }
}
