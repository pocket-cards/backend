{
  "version": "1.0.0",
  "author": "wwalpha <wwalpha@gmail.com>",
  "license": "MIT",
  "scripts": {
    "prebuild": "yarn composeup && sleep 10",
    "build": "yarn data:tables && yarn data:datas",
    "postbuild": "docker commit localstack pocketcards/backend-testing",
    "prestart": "yarn composedown",
    "start": "yarn build",
    "data:tables": "ts-node src/tables.ts",
    "data:datas": "ts-node src/datas.ts",
    "composeup": "docker-compose up -d --build",
    "composedown": "docker-compose down"
  },
  "dependencies": {
    "aws-sdk": "^2.665.0",
    "dynamodb-helper": "https://github.com/wwalpha/dynamodb-helper.git"
  },
  "devDependencies": {
    "@types/node": "^13.13.2",
    "dotenv": "^8.2.0",
    "glob": "^7.1.6",
    "ts-node": "^8.9.1",
    "typescript": "^3.8.3"
  }
}
