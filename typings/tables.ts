export interface GroupsItem {
  id: string;
  nextTime: string;
  word?: string;
}

export interface WordsItem {
  // 単語
  word: string;
  // 発音記号
  pronounce?: string;
  // 語彙（中国語）
  vocChn?: string;
  // 語彙（日本語）
  vocJpn?: string;
  // 音声ファイル
  mp3?: string;
}
