version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build:prod
      - npm run build:zip
  # post_build:
  #   commands:
  #     - npm test
artifacts:
  secondary-artifacts:
    D0_D001:
      files:
        - D0_D001.zip
      name: D0_D001.zip
      base-directory: dist
    D0_D002:
      files:
        - D0_D002.zip
      name: D0_D002.zip
      base-directory: dist

